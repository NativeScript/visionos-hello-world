import { EventData, View } from '@nativescript/core';
import { viewBindings } from '@vision/nativescript-data';

export const Detail = () => {
  // const id = options.id;
  // console.log(options.id)
  return (
    <gridLayout>
      <label text="Hello World Detail" style="font-size:40; color:white;" />
    </gridLayout>
  );
};
